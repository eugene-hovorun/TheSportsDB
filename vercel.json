{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "src/views/**", "src/public/**"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/dist/server.js" }]
}
